## 2026-02-11

- User reported Slack not working; OpenClaw status showed gateway scheduled task stopped even though Slack tokens OK.
- User requested Explorium match prospects skill; created `skills/explorium-prospects-match` with Node script.
- Stored Explorium api key in `secrets/explorium.json` and updated scripts to read from --api-key / env / secrets file.
- Verified Match prospects call works; matched LinkedIn URL to prospect_id.
- Implemented additional script for Contacts Information enrichment: `POST /v1/prospects/contacts_information/enrich`.
- Noted header sensitivity: this enrichment endpoint required `API_KEY` header; using `api_key` caused 401. Retried with correct header and succeeded.
- User requested credit-tracking + key rotation: each key has 100 credits/month; match=0, contacts_information/enrich=5; update Google Sheet column `Credits used/Month`, rotate to next key at 100, and log usage (timestamp etc.). No automatic retries; ask before retry.
- User requested real-time sync from Explorium credits endpoint `/v1/credits` to Google Sheet; added script to sync all keys.
