Design a detailed, production-ready architecture for a Chrome extension that provides live AI assistance during sales calls. The extension should capture a live conversation between a caller (agent) and a prospect from a web call tab, transcribe in real time, detect objections, and show short on-screen script suggestions (popups) to the agent in real time. Include: (1) components (extension modules, backend services, data stores), (2) real-time data flow (audio capture - - detection - response - overlay), (3) privacy/security/compliance requirements (consent, PII redaction, encryption, retention), (4) latency targets and scaling strategy, (5) recommended tech stack with alternatives, (6) failure modes and fallbacks, (7) minimal MVP scope vs. future roadmap. Keep it structured and actionable. 
